alert("Si funciona");
 //window es un objeto por defecto del navegador
    //location es un atributo/propiedad de tipo objet 
    var ruta= window.location;
    console.log(ruta);
    console.log(navigator);
    console.log(window);
    console.log(document);
    document.write("El documento está en :" + ruta);